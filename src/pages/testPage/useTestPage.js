const useTestPage = () => {
    const handleSearch = (value) => {
        if (value) {
            // Call API
        }
    };

    return {
        handleSearch,
    };
};

export default useTestPage;
